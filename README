- Make it executable:
    sudo chmod +x ansible.sh
- Optionally, override username and IPs using export:
    sudo USERNAME=ubuntu \
        ANSIBLE_HOST_IP=192.168.9.128 \
        MASTER_IP=192.168.9.133 \
        WORKER1_IP=192.168.9.136 \
        ./ansible.sh


- Run the script:
    ./ansible.sh